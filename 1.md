classDiagram
    class Cliente {
        -int id
        -string nome
        -date data_nasc
        -Contato contato
        -Endereco endereco
        +cadastrar()
        +alterar()
        +excluir()
        +consultar()
    }

    class Contato {
        -int id
        -string telefone
        -string email
        -string whatsapp
    }

    class Endereco {
        -int id
        -string rua
        -string numero
        -string bairro
        -string cidade
        -string estado
    }

    class ClienteController {
        +salvarCliente()
        +editarCliente()
        +removerCliente()
        +buscarCliente()
    }

    class ClienteBoundary {
        +exibirFormulario()
        +mostrarCliente()
        +solicitarDadosCliente()
        +mostrarMensagem()
    }

    %% Relações
    Cliente "1" --> "1" Contato : possui >
    Cliente "1" --> "1" Endereco : reside em >
    ClienteBoundary --> ClienteController : usa >
    ClienteController --> Cliente : manipula >
